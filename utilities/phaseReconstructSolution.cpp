#include "CommandLine.h"
#include "CgnsUnstructuredGrid.h"

int main(int argc, char *argv[])
{
    CommandLine(argc, argv);
    CgnsUnstructuredGrid solution;

    //- Read the grid file

    //- Find the common nodes as specified by the tolerance and merge them

    //- Find the common elements and merge them

    //- Write the new solution to a file

    return 0;
}
